using UnityEngine;

namespace SweetRush
{
    public class SpawnedObjectBase : MonoBehaviour
    {
        //Potentialy will Destroy smthing spawned
       /* private void Update()
        {
            if (GameManager.Instance._isGameStart) return;
            Destroy(gameObject);
        }*/
        
    }
}